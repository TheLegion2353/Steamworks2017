package org.usfirst.frc.team2353.commands;

import edu.wpi.first.wpilibj.command.CommandGroup;

/**
 *
 */
public class DoNothing extends CommandGroup {

    public DoNothing() {
        /*
         * This literally does nothing. It just exists so that we can pass the SendableChooser method an object because 
         * otherwise it'll throw an error and won't compile.
    	*/
    }
}
